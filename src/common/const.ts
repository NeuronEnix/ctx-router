export const STATS_INTERVAL_MS = 5000; // Stats refresh interval in milliseconds

export const STATS = {
  cpu: -1, // percentage (0-100), -1 if unavailable
  mem: -1, // MB used by process, -1 if unavailable
};
